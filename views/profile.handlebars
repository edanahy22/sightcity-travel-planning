<div>
  <h5 class="center-align">

    <a class="waves-effect waves-light btn-large" href="/newtrip">Create New Trip</a>
    <a class="waves-effect waves-light btn-large" href="/trip">View Saved Trips</a>
  </div>

{{!-- add list of saved trips, maybe make it scrollable??=============================== --}}

  <div>
    {{#each trips as |trip| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{!-- {{{get_emoji}}} --}}
      <a href="/trip/{{trip.id}}">{{trip.location}}</a>
    </h2>
    <p>
      <span for="img" aria-label="pin">📍</span>
      <!-- Pass needed_funding value to the helper function -->
      Trip Start: {{trip.start_date}}<br>
      Trip End: {{trip.end_date}}
    </p>
  </div>
</div>
{{/each}}
  </div>

  <div>

  </div>
</body>
