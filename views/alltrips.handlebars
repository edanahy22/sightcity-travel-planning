<!-- Loop over each object in the projects array -->
{{#each trips as |trip| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/trip/{{trip.id}}">{{trip.location}}</a>
    </h2>
    <p>
      <span for="img" aria-label="pin">📍</span>
      <span>{{trip.start_date}}</span>
      <span>{{trip.end_date}}</span>
    </p>
  </div>
</div>
{{/each}}