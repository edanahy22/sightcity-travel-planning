<div class="text-center">
  <p>{{location}}</p>
  <p>Created by {{user.first_name}} on {{location}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{start_date}}</span>
    <span class="dollar-amount">{{end_date}}</span>
  </p>
</div>